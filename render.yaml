services:
  - type: web
    name: fantasy-command-vision
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: FANTASY_COMMAND_VISION_MODEL
        value: gpt-5.6-terra
